name: novel_app_client
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  image_picker: ^1.0.7
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.4
  app_links: ^7.2.1
  clipboard: ^0.1.3
  share_plus: ^10.1.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.13.1"

flutter:
  uses-material-design: true
  assets:
    - assets/images/bg.jpg

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"
